{
  "entities": {
    "Scan": {
      "title": "Scan Record",
      "description": "A forensic chemical/colorimetric spectrum scan result.",
      "type": "object",
      "properties": {
        "email": {
          "type": "string",
          "description": "The user's local identity public key or email identifier."
        },
        "date": {
          "type": "string",
          "description": "The human-readable date of the scan."
        },
        "location": {
          "type": "string",
          "description": "Physical lab or mobile unit location of the scan."
        },
        "verdict": {
          "type": "string",
          "description": "The overall hazard verdict of the scan."
        },
        "matrix": {
          "type": "string",
          "description": "The substrate matrix being analyzed."
        },
        "detections": {
          "type": "array",
          "description": "Detected chemical signatures."
        },
        "createdAt": {
          "type": "string",
          "description": "ISO timestamp representing when the scan was saved."
        }
      },
      "required": ["email", "date", "location", "verdict", "matrix", "createdAt"]
    },
    "UserProfile": {
      "title": "User Profile",
      "description": "The user's account profile and shipment details.",
      "type": "object",
      "properties": {
        "email": {
          "type": "string",
          "description": "The user's local identity public key or email identifier."
        },
        "username": {
          "type": "string",
          "description": "The user's handle."
        },
        "tier": {
          "type": "string",
          "description": "Subscription tier or membership level."
        },
        "access": {
          "type": "string",
          "description": "Authorization access clearances."
        },
        "shippingName": {
          "type": "string",
          "description": "Full name for shipping physical standards."
        },
        "shippingAddress": {
          "type": "string",
          "description": "Street shipping address."
        },
        "shippingCity": {
          "type": "string",
          "description": "Shipping city."
        },
        "shippingZip": {
          "type": "string",
          "description": "Shipping postal or zip code."
        }
      },
      "required": ["email", "username", "tier", "access"]
    }
  },
  "firestore": {
    "scans/{scanId}": {
      "schema": "Scan",
      "description": "Collection of forensic TLC / colorimetric scan records."
    },
    "users/{email}": {
      "schema": "UserProfile",
      "description": "Profiles storing shipping and tier access keys."
    }
  }
}
